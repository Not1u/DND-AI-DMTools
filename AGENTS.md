# 项目入口

- 根目录只作为本地发布入口；可运行文件为 `SoloTRPG.exe`。
- 开发源码位于 `source/`，修改前阅读 `source/AGENTS.md`、`source/docs/PROJECT_REVIEW.md` 和 `source/docs/DEVELOPMENT.md`。
- GitHub Actions 配置保留在根目录 `.github/`，Git 仓库元数据保留在 `.git/`。
