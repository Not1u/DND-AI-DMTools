# SoloTRPG

双击根目录的 `SoloTRPG.exe` 即可启动当前 Windows 测试版，无需安装。

项目源码、开发文档和历史本地产物已集中在 [`source/`](source/)；开发者从该目录运行 `npm ci`、测试和构建命令。用户存档仍位于 `%APPDATA%/SoloTRPG/campaign`，整理项目目录不会移动或覆盖存档。

完整说明见 [`source/docs/DESKTOP.md`](source/docs/DESKTOP.md)。
